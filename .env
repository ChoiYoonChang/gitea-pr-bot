GITEA_BASE_URL="https://gitea.com"
GITEA_TOKEN="80577cd99c573b58847a433e0515fffd9a11607f"
GITEA_WEBHOOK_SECRET="$(openssl rand -hex 32)"

# ===========================================
# 2. AI 모델 연동 설정 (필수)
# ===========================================
# Ollama 서버 URL (기본값: http://localhost:11434/v1)
LLM_BASE_URL=http://localhost:11434/v1

# AI 모델 API 키 (Ollama는 dummy 값 사용)
LLM_API_KEY=dummy

# 사용할 AI 모델명 (Ollama에서 설치된 모델)
LLM_MODEL=starcoder2:3b

# 백업 모델 (주 모델이 실패할 경우)
LLM_FALLBACK_MODEL=deepseek-coder:1.3b

# ===========================================
# 3. 데이터베이스 설정 (선택사항)
# ===========================================
# 개발 환경: H2 인메모리 DB (기본값)
# 운영 환경: PostgreSQL, MySQL 등

# PostgreSQL 예시
# DATABASE_URL=jdbc:postgresql://localhost:5432/gitea_pr_bot
# DATABASE_USERNAME=prbot_user
# DATABASE_PASSWORD=your_db_password

# MySQL 예시
# DATABASE_URL=jdbc:mysql://localhost:3306/gitea_pr_bot?useSSL=false&serverTimezone=UTC
# DATABASE_USERNAME=prbot_user
# DATABASE_PASSWORD=your_db_password

# ===========================================
# 4. 서버 설정 (선택사항)
# ===========================================
# 서버 포트 (기본값: 8080)
SERVER_PORT=8080

# 컨텍스트 경로 (기본값: /api)
SERVER_CONTEXT_PATH=/api

# ===========================================
# 5. 로깅 설정 (선택사항)
# ===========================================
# 로그 레벨 (DEBUG, INFO, WARN, ERROR)
LOG_LEVEL=INFO

# ===========================================
# 6. 보안 설정 (선택사항)
# ===========================================
# JWT 시크릿 (인증이 필요한 경우)
JWT_SECRET=your_jwt_secret_here

# CORS 허용 도메인
CORS_ALLOWED_ORIGINS=http://localhost:3000,https://your-gitea-domain.com
